title: Home Page

# Extend the `page` scheme
extend: pages.page

layout:
    sections:
        page:
            fields: [title, description, content, heroImage]

fields:
    heroImage:
        type: image
        label: Hero Image
